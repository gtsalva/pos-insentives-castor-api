DB_HOST=localhost
DB_PORT=5432
DB_USER=pos_user
DB_PASSWORD=pos_password_segura
DB_NAME=pos_castor

JWT_SECRET=cambia_esto_min_32_chars_secreto
JWT_EXPIRES_IN=8h

API_PORT=3000
NODE_ENV=development
TZ=America/Guatemala

CORS_ORIGIN_CAJA=http://localhost:4200
CORS_ORIGIN_ADMIN=http://localhost:4201

API_URL=http://localhost:3001

# Storage — STORAGE_PROVIDER=local (dev) | cloudflare (prod)
STORAGE_PROVIDER=local

# Cloudflare R2 (solo producción)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=pos-castor-assets
R2_PUBLIC_URL_PREFIX=https://assets.tudominio.com
